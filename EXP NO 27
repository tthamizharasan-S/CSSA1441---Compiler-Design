%{
#include <stdio.h>
%}

%%
abc         { printf("ABC"); }        /* Replace abc with ABC */
.|\n        { printf("%s", yytext); } /* Print remaining characters */
%%

int yywrap(void) {
    return 1;
}

int main(void) {
    printf("Enter input:\n");
    yylex();
    return 0;
}
