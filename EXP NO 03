%{
#include <stdio.h>
%}

%%
[ \t\n]+                 ;   
"//".*                   ;   
"/*"([^*]|\*+[^*/])*\*+"/"   ;   

[a-zA-Z_][a-zA-Z0-9_]*   { printf("Identifier: %s\n", yytext); }
[0-9]+                   { printf("Number: %s\n", yytext); }
"+"|"-"|"*"|"/"          { printf("Operator: %s\n", yytext); }
.                        { printf("Symbol: %s\n", yytext); }
%%

int yywrap()
{
    return 1;
}

int main()
{
    printf("Enter source code:\n");
    yylex();
    return 0;
}
